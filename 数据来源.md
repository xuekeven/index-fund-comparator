# 基金数据来源

本文档说明项目中基金数据的官方来源、同步脚本、筛选规则和入库方式。数据按以下三类组织：

- 场内基金－上交所
- 场内基金－深交所
- 场外基金

## 目录

- [1. 总体数据流](#1-总体数据流)
- [2. 场内基金－上交所](#2-场内基金上交所)
  - [2.1 脚本 A：基金列表](#21-脚本-a基金列表)
  - [2.2 脚本 B：基金详情](#22-脚本-b基金详情)
  - [2.3 上交所数据入库口径](#23-上交所数据入库口径)
- [3. 场内基金－深交所](#3-场内基金深交所)
  - [3.1 脚本 C：基金列表](#31-脚本-c基金列表)
  - [3.2 脚本 D：基金详情](#32-脚本-d基金详情)
  - [3.3 深交所独立数据入口](#33-深交所独立数据入口)
  - [3.4 实时行情、IOPV 与正式净值](#34-实时行情iopv-与正式净值)
- [4. 场外基金](#4-场外基金)
  - [4.1 产品与份额来源](#41-产品与份额来源)
  - [4.2 脚本 E：基金目录与主数据](#42-脚本-e基金目录与主数据)
  - [4.3 脚本 F：基金详情数据](#43-脚本-f基金详情数据)
  - [4.4 场外基金的数据口径](#44-场外基金的数据口径)
- [5. API 展示与衍生字段](#5-api-展示与衍生字段)
- [6. 定时任务](#6-定时任务)
- [7. 当前覆盖与限制](#7-当前覆盖与限制)
- [8. 手动执行脚本 A–F](#8-手动执行脚本-af)

## 1. 总体数据流

生产环境使用 PostgreSQL，API 通过 SQLAlchemy 读取数据库，不在页面请求过程中临时抓取交易所或证监会网站。

```text
官方页面及其结构化接口
        ↓
定时同步脚本
        ↓
PostgreSQL
        ↓
FastAPI
        ↓
前端基金列表和比较页面
```

主要数据表：

| 数据 | 数据表 |
|---|---|
| 基金主体 | `fund_product` |
| 基金份额 | `fund_share_class` |
| 场内上市信息 | `fund_listing` |
| 管理费、托管费等 | `fee_history` |
| 单位净值、累计净值 | `nav_daily` |
| 收盘价及行情 | `market_quote` |
| 基金规模 | `fund_scale` |
| 区间收益率 | `calculated_metric` |

生产环境必须使用 `IFC_DATA_MODE=database`。`sample` 模式只返回开发演示数据。

## 2. 场内基金－上交所

上交所场内基金拆成两个独立任务：

| 任务 | 职责 | 执行时间（北京时间） |
|---|---|---|
| 脚本 A | 读取上交所 ETF 列表，并按指数名称和排除词筛选目标基金；创建或更新基金主体、基金份额和上交所上市记录；维护基金代码、名称、管理人、ETF 结构、交易场所、目标指数归类、精确跟踪基准、上市日期、详情页分类、来源时间和在库状态 | 每周一 09:00 |
| 脚本 B | 遍历脚本 A 筛出的基金详情页；更新开盘价、最高价、最低价、收盘价、成交量、成交额和交易日；同步正式净值及历史净值、管理费率、托管费率和人民币基金规模；计算近 1 月、近 3 月、近 6 月、今年来和近 1 年收益率 | 周一至周五 16:00 |

### 2.1 脚本 A：基金列表

实现文件：`backend/app/sync/sse_funds.py`

官方入口：<https://etf.sse.com.cn/fundlist/>

实际读取接口：<https://query.sse.com.cn/commonQuery.do>

请求口径：

| 参数 | 值 |
|---|---|
| `sqlId` | `COMMON_JJZWZ_JJLB_L` |
| `isPagination` | `true` |
| `pageHelp.pageSize` | `2000` |
| `CATEGORY` | `F000` |
| `type` | `inParams` |
| `Referer` | `https://etf.sse.com.cn/fundlist/` |

脚本读取 `result` 数组中的以下字段：

| 原始字段 | 用途 |
|---|---|
| `FUND_CODE` | 基金代码、上市代码 |
| `FUND_ABBR` | 场内简称 |
| `FUND_EXPANSION_ABBR` | 页面优先展示的基金名称 |
| `COMPANY_NAME` | 基金管理人 |
| `INDEX_NAME` | 目标指数筛选 |
| `LISTING_DATE` | 上市日期 |
| `CATEGORY` | 生成具体基金详情页 URL |

目标基金筛选规则：

1. `INDEX_NAME` 去除首尾空白后，必须与下表中的官方名称精确相等，不使用简称模糊匹配；
2. 将 `FUND_ABBR`、`FUND_EXPANSION_ABBR` 和 `INDEX_NAME` 拼接为检查文本；
3. 按基金代码 `FUND_CODE` 去重后写入数据库，其他指数或无法识别的记录不纳入。

| 目标分类 | `INDEX_NAME` 纳入值 | 排除规则 |
|---|---|---|
| 中证500 | `中证小盘500指数`、`中证500指数` | 检查文本包含“增强、低波、等权、质量、价值、成长、现金流、信息”中的任意一个即排除 |
| 标普500 | `标准普尔500指数`、`标普500指数` | 当前没有额外名称排除词 |
| 纳斯达克100 | `纳斯达克100指数`、`纳斯达克-100指数` | 当前没有额外名称排除词 |

脚本 A 写入或更新基金主体、基金份额和上交所上市信息，不再写入净值、行情、费率或基金规模。列表接口即使返回 `SCALE`，项目也不使用该字段；基金规模统一由脚本 B 从具体详情页读取。

详情页 URL 使用基金代码和分类，例如：

<https://etf.sse.com.cn/fundlist/funddetail/index.shtml?code=510500&category=F112>

### 2.2 脚本 B：基金详情

实现文件：`backend/app/sync/sse_details.py`

脚本 B 从数据库读取脚本 A 已筛选出的所有上交所目标基金，然后逐只访问其具体详情页使用的三个官方接口。

#### 基本信息接口

官方页面：对应基金的上交所详情页。

实际读取接口：<https://query.sse.com.cn/commonQuery.do>

| 参数 | 值 |
|---|---|
| `sqlId` | `COMMON_JJZWZ_JJLB_JJXQ_JBXX_C` |
| `FUND_CODE` | 当前基金代码 |
| `Referer` | 当前基金的具体详情页 |

读取字段：

| 原始字段 | 入库用途 |
|---|---|
| `MANAGEMENT_RATE` | 管理费率，单位为百分比 |
| `TRUSTEESHIP_RATE` | 托管费率，单位为百分比 |
| `SCALE` | 基金规模，单位为亿元 |

#### 详情页快照接口

实际读取接口：

```text
https://yunhq.sse.com.cn:32042/v1/sh1/snap/<基金代码>
```

请求的 `select` 字段顺序为：

```text
name,last,chg_rate,change,open,prev_close,high,low,volume,amount,
tradephase,cpxxextendname,iopv,fp_volume,fp_amount,fp_phase,cpxxsubtype
```

读取字段：

| 响应位置 | 页面含义 | 入库用途 |
|---|---|---|
| `date` | 详情页交易日 | 行情日、净值日、规模日期 |
| `snap[1]` / `last` | 最新价格；收盘后即当日收盘价 | `market_quote.close_price` |
| `snap[4]` / `open` | 开盘价 | `market_quote.open_price` |
| `snap[6]` / `high` | 最高价 | `market_quote.high_price` |
| `snap[7]` / `low` | 最低价 | `market_quote.low_price` |
| `snap[8]` / `volume` | 成交量 | `market_quote.volume` |
| `snap[9]` / `amount` | 成交金额 | `market_quote.turnover_amount` |
| `snap[12]` / `iopv` | 详情页标注的“净值” | `nav_daily.unit_nav` |

上交所详情页把 `iopv` 展示为“净值”，项目按该页面口径保存。申购赎回清单中的 `NAV/PRE_TRADING_DAY` 是另一套口径，不再用于基金列表页面。

#### 历史净值接口与区间收益率

实际读取接口：

```text
https://yunhq.sse.com.cn:32042/v1/sh1/dayk/<基金代码>
```

首次为某只基金同步时，脚本使用 `begin=-320`、`end=-1`、`period=day` 和
`select=date,iopv,prevClose` 回填约 320 个交易日。响应中的 `kline[][0]`
是交易日，`kline[][1]` 是详情页口径净值，逐日写入 `nav_daily`。

完成首次回填后，日常任务不再重复请求整段历史；详情页快照中的当日净值会持续
追加到 `nav_daily`。

脚本按最新净值日计算以下五项简单收益率，不考虑分红复权：

| 指标 | 起点 |
|---|---|
| 近 1 月 | 最新净值日前推 1 个月，当日或之前最近一个净值日 |
| 近 3 月 | 最新净值日前推 3 个月，当日或之前最近一个净值日 |
| 近 6 月 | 最新净值日前推 6 个月，当日或之前最近一个净值日 |
| 今年来 | 上一自然年最后一个净值日 |
| 近 1 年 | 最新净值日前推 1 年，当日或之前最近一个净值日 |

统一公式为 `(期末净值 ÷ 期初净值 − 1) × 100%`，结果以
`return_1m`、`return_3m`、`return_6m`、`return_ytd` 和
`return_1y` 写入 `calculated_metric`，质量状态为 `estimated`。

#### 脚本 B 的事务规则

- 每只基金的收盘价、净值和规模统一使用快照接口的 `date` 作为交易日；
- `SCALE × 1 亿`写入人民币规模；
- 管理费和托管费写入费率历史，值不变时刷新采集时间，值变化时关闭旧记录并新增记录；
- 行情、净值和规模按基金及交易日更新，重复运行不会重复生成同日行情或净值；
- 历史净值只在首次缺少回填标记时整段读取，日常运行使用详情页快照增量入库；
- 五个区间收益率每天根据本地 `nav_daily` 重新计算并写入 `calculated_metric`；
- 所有指标的 `source_url` 都保存当前基金的具体详情页；
- 每只基金使用独立保存点更新；单只基金返回无效关键数据或请求失败时，仅回滚该基金并记录警告，其他基金继续同步；如果本次没有任何基金成功，整个任务失败并回滚。

### 2.3 上交所数据入库口径

| 页面字段 | 数据库字段 | 日期字段 |
|---|---|---|
| 收盘价 | `market_quote.close_price` | `market_quote.trade_date` |
| 净值 | `nav_daily.unit_nav` | `nav_daily.nav_date` |
| 管理费 | `fee_history.rate`，类型 `management` | 采集时间 |
| 托管费 | `fee_history.rate`，类型 `custody` | 采集时间 |
| 基金规模 | `fund_scale.amount_cny` | `fund_scale.report_date` |

页面中的运作费率不是单独抓取的字段，而是管理费率与托管费率之和。

## 3. 场内基金－深交所

深交所场内基金同样拆成两个独立任务：

| 任务 | 职责 | 执行时间（北京时间） |
|---|---|---|
| 脚本 C | 读取深交所 ETF 全部分页，清理名称并按基金代码去重，再按拟合指数代码和排除词筛选目标基金；创建或更新基金主体、基金份额和深交所上市记录；维护基金代码、名称、管理人、ETF 结构、交易场所、目标指数归类、精确跟踪基准、来源时间和在库状态；关联证监会 EID 基金记录，并从最新版产品资料概要 PDF 中提取和更新管理费率、托管费率 | 每周一 09:00 |
| 脚本 D | 遍历脚本 C 筛出的目标基金；同步深交所最近交易日行情并写入收盘价及行情历史；合并深交所近期净值与证监会 EID 历史净值并写入净值历史；根据官方份额规模和同日净值估算人民币基金规模；计算近 1 月、近 3 月、近 6 月、今年来和近 1 年收益率；为页面计算收盘价相对净值的偏离提供数据，其中 QDII 只使用中国交易日对应的前一海外交易日净值，缺少对应日期净值时不计算偏离 | 周一至周五 22:00 |

### 3.1 脚本 C：基金列表

实现文件：`backend/app/sync/szse_funds.py`

以下说明脚本 C 使用的官方列表接口、字段和筛选规则。

官方入口：<https://www.szse.cn/www/market/product/list/etfList/index.html>

实际读取接口：<https://www.szse.cn/api/report/ShowReport/data>

主要请求参数：

| 参数 | 值 |
|---|---|
| `CATALOGID` | `1945` |
| `TABKEY` | `tab1` |
| `SHOWTYPE` | `JSON` |
| `PAGENO` | 当前页码 |
| `txtQueryKeyAndJC` | 中证500、标普500、纳指等搜索词 |

脚本分别使用“中证500、标普500、纳指、纳斯达克、纳斯达克100”作为列表搜索词，读取每个搜索结果的全部分页；“纳斯达克”用于覆盖 `159632`“纳斯达克ETF华安”等简称中不带“100”或“纳指”的基金，最终仍严格要求拟合指数 `nhzs` 等于 `NDX`；去除响应字段中的 HTML 标签并解码实体后，按证券代码 `sys_key` 去重。主要字段包括证券代码、证券简称、拟合指数、基金管理人和官方快照日期。

目标基金筛选规则：

1. 将拟合指数 `nhzs` 删除空格并转为大写；
2. 按下表判断目标指数，未命中的记录不纳入；
3. 中证500的排除词只检查证券简称 `kzjcurl`，标普500和纳斯达克100当前没有额外名称排除词。

| 目标分类 | `nhzs` 纳入条件 | 排除规则 |
|---|---|---|
| 中证500 | 以 `000905` 或 `399905` 开头 | 证券简称包含“增强、成长、价值、低波、等权、质量、现金流、信息”中的任意一个即排除 |
| 标普500 | 以 `SPXNTR` 开头 | 当前没有额外名称排除词 |
| 纳斯达克100 | 必须严格等于 `NDX` | 当前没有额外名称排除词；纳指科技等其他指数代码不会纳入 |

深交所没有上交所那种把行情、净值、费率和规模集中在一起的统一基金详情页。ETF 列表把不同内容分别链接到独立页面：

| 列表入口 | 内容 | 以 `159922` 为例 |
|---|---|---|
| 基金代码 | 行情页面 | <https://www.szse.cn/market/trend/index.html?code=159922> |
| 基金名称 | ETF 公告 | <https://www.szse.cn/disclosure/fund/etf/index.html?stock=159922&name=中证500ETF嘉实> |
| 申赎清单图标 | 申购赎回清单 | <https://www.szse.cn/disclosure/fund/currency/index.html?txtJCorDH=159922> |

上述链接由列表响应提供，不应把其中任一页面理解为包含全部字段的“基金详情页”。

深交所列表的当前规模单位是“万份”，不是人民币资产规模。脚本 C 不直接写入人民币规模；脚本 D 使用规模日期当日或之前最近一日的正式单位净值，按“万份 × 10,000 × 单位净值”估算人民币规模，并标记为 `estimated`。

管理费和托管费属于低频变化数据。脚本 C 通过基金代码映射证监会基金电子披露平台的官方基金详情，每周读取最新“基金产品资料概要”PDF并解析现行年费率。文件缺失或解析失败时只记录警告，不使用猜测值覆盖数据库现有费率，也不回滚该次基金主数据更新。

### 3.2 脚本 D：基金详情

实现文件：`backend/app/sync/szse_details.py`；旧的 `backend/app/sync/szse_quotes.py` 保留为仅同步行情的兼容命令，不再由定时任务调用。

该同步器作为深交所脚本 D，周一至周五 22:00 执行，遍历脚本 C 已筛选出的目标基金。

官方入口：<https://www.szse.cn/www/market/product/list/etfList/index.html>

实际读取接口：<https://www.szse.cn/api/report/ShowReport/data>

脚本读取官方 ETF 行情中的最近约 10 个交易日、收盘价和成交金额，逐日写入 `market_quote`。最新收盘价用于列表展示。非 QDII 基金仅在收盘价与净值日期相同时计算“偏离”；QDII 基金使用中国交易日收盘价与其对应的前一海外交易日净值计算，若缺少该净值（例如缺少 `08/27`），则主动隐藏偏离，不会退回使用 `08/26` 的过期净值。单只基金行情或详情请求失败时会记录警告并继续处理其他基金。

### 3.3 深交所独立数据入口

深交所官网还提供基金公告、ETF 公告、封闭式基金净值、开放式基金净值和申购赎回清单等独立栏目：

<https://www.szse.cn/market/fund/list/stockFundList/index.html>

ETF 最近净值可以从深交所报表接口读取：

```text
GET https://www.szse.cn/api/report/ShowReport/data
SHOWTYPE=JSON
CATALOGID=1785_child
TABKEY=tab1
txtDm=<基金代码>
```

主要响应字段：

| 原始字段 | 含义 |
|---|---|
| `fund_code` | 基金代码 |
| `security_short_name` | 证券简称 |
| `nav_date` | 净值日期 |
| `nav_per_share` | 每份基金净值 |

该接口当前只返回最近约 10 个交易日，用于取得深交所官网最新净值并与另一官方来源交叉覆盖。为了让新部署实例立即具备长周期历史，脚本 D 还按基金代码查询中国证监会基金电子披露平台的 <http://eid.csrc.gov.cn/fund/disclose/getPublicFundJZInfoMore.do>，一次回填最近 400 个自然日的正式单位净值；单只 ETF 一年通常少于接口的 500 条上限。脚本取两边日期的并集：证监会日期更新就使用证监会最新值，深交所日期更新就使用深交所最新值；相同净值日两边都有时优先使用深交所值。合并后逐日写入 `nav_daily`，再计算近 1 月、近 3 月、近 6 月、今年来和近 1 年简单收益率。

规模使用 ETF 列表的“万份”规模与规模日期当日或之前最近一日的正式单位净值估算人民币资产规模。通常电子披露平台可补齐同日净值；若同日尚未披露则使用不晚于规模日期的最近值。该值不是基金定期报告披露的审计规模，因此写入 `fund_scale` 时质量状态为 `estimated`。

### 3.4 实时行情、IOPV 与正式净值

深交所 ETF 的“实时交易”“盘中参考净值”和“正式基金净值”是三类不同数据，不能混用：

| 数据 | 含义 | 获取难度与使用建议 |
|---|---|---|
| 盘中成交行情 | 最新成交价、成交量、成交金额、买卖盘等二级市场行情 | 数据本身容易获得，但官方真正实时行情属于行情服务；公开网页通常是延时展示，长期稳定接入应使用获授权的 Level-1 行情源。当前项目只同步日行情，不采集盘中数据 |
| IOPV | 基于申购赎回清单和组合证券行情计算的盘中基金份额参考净值 | 深交所在交易时间通过行情系统高频发布，但它是估计值，不是收盘后确认的正式 NAV。适合盘中估算溢折价，不应写入 `nav_daily` 代替正式净值 |
| 正式基金净值 | 基金管理人计算并披露的每份基金净值 | 收盘后从上一节的最近净值接口获取较容易，适合列表展示、历史积累和区间收益率计算 |

本项目当前目标是日频比较，不需要接入真正实时行情。建议在收盘后运行日任务：读取当日收盘价，再读取已经披露的正式净值；若净值尚未发布则保留为空并在下一次任务重试。将来如增加盘中页面，应把成交行情和 IOPV 存入独立的盘中数据表，并显示数据时间和“参考净值”标识。

## 4. 场外基金

### 4.1 产品与份额来源

实现文件：`backend/app/sync/csrc_funds.py`

场外基金使用中国证监会及基金电子披露平台的官方数据：

| 数据 | 官方来源 | 实际地址 |
|---|---|---|
| 公募基金产品索引 | 中国证监会 | <https://www.csrc.gov.cn/csrc/c101900/c1029655/content.shtml> 中最新 `.xlsx` 附件 |
| 基金代码校验 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/validate_fund.do> |
| 基金详情 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/fund_detail.do> |
| 基金产品资料概要 | 基金详情页“基金产品资料概要”PDF | 详情页内的 `instance_show_pdf_id.do?instanceid=...` 链接 |
| 最新季度报告 | 基金详情页“季度报告”PDF | 详情页内最新的 `instance_show_pdf_id.do?instanceid=...` 链接 |
| 最新可用估值日 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/publicDailyReportSearchData.json> |
| 份额与净值 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/getPublicFundJZInfoMore.do> |

电子披露接口当前使用 HTTP。同步器只提交公开基金查询条件，不发送账号、密码或个人数据。

页面的基金代码链接指向该基金独立的电子披露详情页 `fund_detail.do?fundId=...`。产品索引 `.xlsx` 只用于脚本 E 发现候选基金，不作为单只基金的详情链接。

场外候选产品从最新 `.xlsx` 的活动工作表第 3 行开始读取：第 B 列是基金代码、第 C 列是基金简称、第 D 列是成立日期。具体筛选规则如下：

1. 数字代码补足为 6 位；不是 6 位数字的记录排除；
2. 不再根据 `15、16、50、51、52、56、58` 等两位代码前缀直接排除；历史代码可能在产品转型后继续使用，代码段不能代表当前是否上市；
3. 基金名称删除所有空白、统一中英文括号并转成大写后再匹配；
4. 索引简称或详情全称包含 `LOF` 的产品排除；包含 `ETF` 但不包含“联接”的场内 ETF 排除；包含 `ETF` 且包含“联接”的产品标记为“ETF联接基金”，其余纳入产品标记为“普通开放式指数基金”；
5. 按下表进行目标指数名称匹配和变体排除，未命中的产品不纳入候选列表；
6. 初筛候选用基金代码换取电子披露平台 `fundId`，再以详情页基金全称重复执行第 4、5 步，防止目录简称过短或历史代码沿用导致误判；
7. 通过上述两轮名称规则的产品作为脚本 E 的场外目标。脚本 F 读取产品概要时还会以“上市交易所及上市日期”和“运作方式”复核；明确为交易型开放式或上市开放式的产品不会写入详情数据。

| 目标分类 | 名称纳入条件 | 任意命中即排除的名称词 |
|---|---|---|
| 中证500 | 包含“中证500” | 增强、量化、优选、智选、行业中性、低波、等权、质量、成长、价值、基本面、ESG、信息、自由现金流、策略 |
| 标普500 | 包含“标普500” | 等权、低波、质量、增强、策略、科技、生物 |
| 纳斯达克100 | 包含“纳斯达克100”或“纳指100” | 等权、低波、质量、增强、策略、科技、生物 |

例如，`160213`、`164809` 的详情全称符合场外指数基金规则，因此保留；`513110` 的详情全称是场内 ETF，因此排除。脚本 F 再从净值、产品概要和申购公告发现同一产品实际存在的 A、C、E、人民币、美元现汇或美元现钞等份额；份额发现不会把同一产品的不同份额误当成新的基金主体。

### 4.2 脚本 E：基金目录与主数据

实现文件：`backend/app/sync/csrc_funds.py`

脚本 E 是场外基金的低频目录任务，每月 1 日 09:00 执行：

1. 从证监会产品索引取得候选基金代码和产品快照；
2. 按 4.1 的代码、产品结构、目标指数和排除词规则筛选候选产品；
3. 用基金代码换取官方 `fundId`；
4. 读取基金详情页中的基金全称、管理人和合同生效日期，再按 4.1 规则复核；
5. 以 `csrc:{fundId}` 为基金主体唯一键，更新目标指数、产品结构、管理人、成立日期、状态及来源；
6. 以目录基金代码建立或更新主份额。其他 A/C/E、人民币及美元份额由脚本 F 从详情来源发现；
7. 只有当本轮所有候选都无失败时，才把历史中已不符合本月筛选规则的场外产品及其份额标记为 inactive；存在请求失败时不执行停用，避免把暂时抓取失败误判为退场。

主要字段映射：

| 官方内容 | 入库用途 |
|---|---|
| 产品索引基金代码、简称、成立日期 | 候选产品筛选及产品快照 |
| 详情页基金名称、基金管理人、合同生效日期 | 基金主体主数据 |
| 详情页基金全称、基金管理人、合同生效日期 | 复核目标并更新基金主体主数据 |
| 目录基金代码及详情页简称 | 建立或更新主份额 |

脚本 E 不读取产品概要、季度报告、净值或申购公告，也不写入费率、规模、净值、申购状态和收益率。

### 4.3 脚本 F：基金详情数据

实现入口：`backend/app/sync/csrc_details.py`

详情同步实现：`backend/app/sync/csrc_funds.py` 的 `run_details_sync`

公共披露 PDF 工具：`backend/app/sync/eid_disclosures.py`。脚本 C 和脚本 F 共用披露链接筛选、PDF 转文字、费率解析和费率历史入库逻辑。

基金公司官方净值兜底：`backend/app/sync/company_official_nav.py`。

脚本 F 在周一至周五 22:00 执行，只遍历脚本 E 已筛选且状态为 active 的场外目标：

1. 读取基金详情页和最新基金产品资料概要 PDF，发现各份额并更新完整业绩比较基准、管理费、托管费和销售服务费；
2. 读取最新季度报告，按份额代码写入“期末基金资产净值”和报告期末日期；
3. 以证监会 EID 净值接口为主数据源。首次缺少一年历史时，把 400 个自然日拆成多个 45 天窗口回填；以后只重读最近 45 天，依赖唯一键幂等覆盖；
4. 对 EID 没有返回净值或一年历史不足的美元份额，按基金管理人访问对应基金公司官网的正式历史净值接口；首次回填 400 个自然日，以后重读最近 45 天。同一份额同一净值日两边都有时仍优先 EID；
5. 从产品概要、净值和申购公告共同发现 A/C/E、人民币、美元现汇或美元现钞份额；
6. 筛选标题含“申购、定投、金额、限制、暂停、恢复、大额”等关键词的业务公告，排除节假日安排；按公告标题、份额类别和正文中的金额限制解析最新有效状态，分别写入开放申购、暂停申购或限额金额；无法从官方公告确认时不再默认写成开放申购；
7. 以各份额本地 `nav_daily` 历史计算近 1 月、近 3 月、近 6 月、今年来和近 1 年收益率。

当前接入的基金公司官方净值兜底均为基金公司自身公开页面或其页面调用的接口：

| 基金公司 | 官方核验页面 | 脚本读取地址 |
|---|---|---|
| 广发基金 | <https://www.gffunds.com.cn/funds/?fundcode=000055> | `https://www.gffunds.com.cn/apistore/JsonService` |
| 华夏基金 | <https://www.chinaamc.com/fund/015518/jijinfeilv_jg.shtml> | `https://www.chinaamc.com/fund/{代码}/zoust_all.js` |
| 博时基金 | <https://www.bosera.com/fund/016056.html> | `https://www.bosera.com/fund/fundHisDetail.json` |
| 嘉实基金 | <https://www.jsfund.cn/Services/cn/jsp/product/basic.jsp?FundCode=016534&SiteID=1> | `https://www.jsfund.cn/servlet/json` |
| 华安基金 | <https://www.huaan.com.cn/funds/040047/> | `https://www.huaan.com.cn/funddetail/selectFundayByCode.do` |
| 摩根基金 | <https://www.cifm.com/fund/017643/> | `https://www.cifm.com/images/year/net_value_his365.xml` |
| 汇添富基金 | <https://www.99fund.com/main/products/pofund/018968/fundnav.shtml> | `https://static.99fund.com/productcenter/v1/new/compose/funds/single/curve/yield/start-to-end/index/collections` |

兜底只接受官网明确返回的份额代码、净值日期、单位净值和累计净值，不做人民币/美元汇率换算，不使用人民币份额反推美元份额，也不从涨跌幅估算净值。基金公司官网也没有该份额净值时，数据库保持为空并记录警告。

主要字段映射：

| 官方内容 | 入库用途 |
|---|---|
| 净值记录中的份额代码、简称、币种 | 发现并更新基金份额 |
| 产品资料概要的业绩比较基准 | 基金精确比较基准说明 |
| 产品资料概要的管理费、托管费、销售服务费 | `fee_history` 当前费率 |
| 季度报告的期末基金资产净值 | `fund_scale` 份额规模，币种为人民币 |
| 单位净值、累计净值 | `nav_daily` 正式历史净值 |
| 申购、定投、暂停、恢复、大额及金额限制公告 | `sales_limit_history` 当前申购状态和限额；按份额分别生效，未知状态不推定为开放 |
| 本地历史单位净值 | `calculated_metric` 五个区间简单收益率 |

同一产品的不同份额共享基金主体，但分别建立份额、费率、规模、净值、申购状态和收益率记录。场外基金没有交易所成交价，脚本 F 不采集或计算成交量、IOPV、溢折价或“偏离”。

份额展示名称采用官方完整份额名称，不使用产品级简称拼接。来源优先级依次为：产品资料概要中的“下属基金简称”、申购公告中的“下属基金份额简称”、净值接口简称。前一来源缺少某个份额时才使用后一来源，因此 A/C/E 和人民币、美元现汇或美元现钞等标识均来自官方披露内容，不针对单只基金补标签。

### 4.4 场外基金的数据口径

普通场外开放式基金没有证券交易所的连续竞价，因此不存在可采集的实时成交价、成交量或买卖盘。投资者提交的是申购、赎回申请，成交价格最终依据对应开放日闭市后计算的基金份额净值确定。

| 数据 | 官方口径 | 获取方案 |
|---|---|---|
| 实时交易价 | 不存在 | 页面不展示，也不使用第三方“实时估值”冒充成交价或正式净值 |
| 正式单位净值、累计净值 | 每个开放日闭市后计算，不晚于次日披露 | 优先使用基金电子披露平台；EID 缺失或历史不足的美元份额使用基金公司官网正式净值兜底，仍缺失则保持为空 |
| 五个区间收益率 | 由历史正式净值计算 | 首次分窗回填 400 个自然日，以后重读最近 45 天，再从本地 `nav_daily` 计算近 1 月、近 3 月、近 6 月、今年来和近 1 年 |

证监会净值接口对约一年跨度的单次查询会返回空结果，因此脚本 F 固定按不超过 45 天的窗口读取后在本地合并。基金公司接口按各官网实际分页方式读取。暂不考虑分红时使用单位净值简单收益；以后如需与基金公司披露的净值增长率严格一致，再增加分红复权处理。

## 5. API 展示与衍生字段

后端读取实现：`backend/app/repository.py`

API 路由：`backend/app/main.py`

前端列表实现：

- `frontend/components/comparison-dashboard.tsx`
- `frontend/components/fund-table.tsx`

展示规则：

- “同步时间”是当前指数类别相关基金数据中最大的 `collected_at`；
- “交易日”只取当前筛选结果中接口实际返回的收盘价日期和净值日期的最新值，不使用同步采集时间、基金列表快照日期或规模日期；
- 表格行内不再分别显示收盘日、净值日和规模日期；
- 运作费率 = 管理费率 + 托管费率 + 销售服务费率；没有销售服务费的份额按 0 计算且页面不显示“销售服务费 0.00”；
- “偏离”使用最新净值日对应的同日收盘价计算`(收盘价 ÷ 净值 − 1) × 100%`；页面仍单独展示最新交易日的收盘价；
- 基金规模统一以亿元展示；
- 上交所场内基金展示近 1 月、近 3 月、近 6 月、今年来和近 1 年简单收益率；
- 当前收益率使用详情页净值计算，不进行分红复权。

该“交易日”规则统一适用于脚本 A–F 产生的数据：

| 脚本 | 对页面“交易日”的影响 |
|---|---|
| A、C、E | 只更新基金列表或主数据，不参与交易日计算 |
| B | 取上交所实际返回的收盘价日期与净值日期中的较新日期 |
| D | 取深交所收盘价日期与深交所/证监会合并后最新净值日期中的较新日期 |
| F | 场外基金没有收盘价，只取证监会正式净值日期 |

## 6. 定时任务

Home Server 使用 `Asia/Shanghai` 时区执行以下任务：

时间配置保存在 `deploy/schedules.conf`，由 `deploy/manage-crontab.sh install` 安装到部署用户的 crontab。脚本使用项目标记块幂等更新，并在迁移时清理旧版未标记的同名同步任务，避免重复执行。若系统提供 `flock`，安装后的命令还会阻止同一同步任务并发运行。

| 时间 | 命令 | 作用 |
|---|---|---|
| 每周一 09:00 | `.venv/bin/python -m app.sync.sse_funds` | 上交所脚本 A |
| 周一至周五 16:00 | `.venv/bin/python -m app.sync.sse_details` | 上交所脚本 B |
| 每周一 09:00 | `.venv/bin/python -m app.sync.szse_funds` | 深交所脚本 C |
| 周一至周五 22:00 | `.venv/bin/python -m app.sync.szse_details` | 深交所脚本 D |
| 每月 1 日 09:00 | `.venv/bin/python -m app.sync.csrc_funds` | 场外基金脚本 E：目录与主数据 |
| 周一至周五 22:00 | `.venv/bin/python -m app.sync.csrc_details` | 场外基金脚本 F：详情数据 |

任务工作目录为：

```text
/home/xuekai/repos/index-fund-comparator/backend
```

同步日志写入：

```text
/home/xuekai/logs/index-fund-sync.log
```

脚本都支持 `--dry-run`。正式任务不使用该参数。

## 7. 当前覆盖与限制

- 上交所目标 ETF 已覆盖列表、收盘价、历史净值、管理费、托管费、基金规模、交易日及五个区间收益率；
- 深交所目标 ETF 已覆盖列表、最近行情、最近 400 日正式净值回填、管理费、托管费、估算规模、同日偏离及五个区间收益率；
- 场外脚本 E 已覆盖证监会目录筛选、基金主体和目录主份额；脚本 F 已覆盖完整份额、精确比较基准、管理费、托管费、销售服务费、最新季度人民币份额规模、正式历史净值、申购状态和五个区间收益率；
- 场外基金没有交易所交易价，因此不展示或计算偏离；
- 上交所五个区间收益率暂按净值简单涨跌计算，不考虑期间分红；
- 跟踪误差不再在基金列表展示，当前也不执行跟踪误差计算任务；
- 上交所详情页接口、深交所报表接口和证监会电子披露接口都不是承诺长期稳定的公开 API，字段或接口标识变化时需要同步更新采集器；
- 当前没有保存每次官方原始响应，数据库保存的是解析后的结构化结果和来源 URL。


## 8. 手动执行脚本 A–F

先进入后端目录：

```bash
cd /home/xuekai/repos/index-fund-comparator/backend
```

按需执行对应命令：

| 脚本 | 内容 | 正式执行命令 |
|---|---|---|
| 脚本 A | 同步上交所基金列表和主数据 | `.venv/bin/python -m app.sync.sse_funds` |
| 脚本 B | 同步上交所基金详情数据 | `.venv/bin/python -m app.sync.sse_details` |
| 脚本 C | 同步深交所基金列表、主数据和费率 | `.venv/bin/python -m app.sync.szse_funds` |
| 脚本 D | 同步深交所行情、净值、规模和区间收益率 | `.venv/bin/python -m app.sync.szse_details` |
| 脚本 E | 从证监会目录筛选场外目标，更新基金和主份额 | `.venv/bin/python -m app.sync.csrc_funds` |
| 脚本 F | 更新场外目标的费率、季度规模、净值、申购状态和区间收益率 | `.venv/bin/python -m app.sync.csrc_details` |

正式执行成功时，最后一行输出类似下面内容。数量和日期以实际运行结果为准：

```text
# 脚本 A
SSE target ETF rows synced: 22

# 脚本 B
SSE target ETF details synced: 22; trade dates: 2026-08-24

# 脚本 C
SZSE target ETF rows synced: 15 for 2026-08-24

# 脚本 D
SZSE target ETF details synced: 15 funds, 150 quotes, 3970 NAV rows, 15 scales, 0 warnings

# 脚本 E
CSRC off-exchange script E catalog synced: 41 products, 41 primary shares, snapshot 2026-06-30, 0 failures

# 脚本 F
CSRC off-exchange script F details synced: 1 products, 2 shares, 2 fee shares, 2 scales, 534 NAV rows, 2 subscription states, 10 return metrics, 0 failures, 0 warnings
```

出现 `synced`、没有异常堆栈并正常返回命令提示符，表示命令执行完成。还可以紧接着执行 `echo $?` 检查退出码；输出 `0` 表示成功。`0 failures` 表示目标产品均完成同步；`warnings` 大于 0 表示存在非致命的数据缺口，应查看紧随其后的警告。上面的 E 是 2026-08-27 全量实跑结果；F 是同日对 `016968` 的定向实跑结果。全量 F 的数量取决于当期目标和各官方披露的可用性。

只验证读取和解析、不写入数据库时，在对应命令末尾增加 `--dry-run`，例如：

```bash
.venv/bin/python -m app.sync.szse_details --dry-run
```


脚本 E 和 F 都支持可重复的 `--code`。E 的代码必须是目录候选主代码；F 可传目标产品任一份额代码：

```bash
.venv/bin/python -m app.sync.csrc_funds \
  --code 160213 \
  --code 164809

.venv/bin/python -m app.sync.csrc_details --code 050025
```

首次完整同步或需要重建数据时，应先执行列表脚本，再执行详情脚本：上交所按 A → B，深交所按 C → D，场外基金按 E → F。
