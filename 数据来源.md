# 基金数据来源与核验手册

> 文档基线：2026-08-15 当前工作区代码与 Home Server 共享数据库。官方页面和接口可能调整；修改同步器、字段口径或数据源后，应同步更新本文。

## 目录

- [1. 结论](#1-结论)
- [2. 数据流](#2-数据流)
- [3. 官方数据源总表](#3-官方数据源总表)
- [4. 各同步器如何读取和处理](#4-各同步器如何读取和处理)
  - [4.1 上交所 ETF 清单和规模](#41-上交所-etf-清单和规模)
  - [4.2 上交所日行情](#42-上交所日行情)
  - [4.3 深交所 ETF 清单](#43-深交所-etf-清单)
  - [4.4 深交所日行情](#44-深交所日行情)
  - [4.5 证监会场外基金、份额和净值](#45-证监会场外基金份额和净值)
- [5. 指数归类与基准口径](#5-指数归类与基准口径)
- [6. 页面字段从哪里来](#6-页面字段从哪里来)
- [7. 数据库表与可追溯字段](#7-数据库表与可追溯字段)
- [8. 当前数据库快照](#8-2026-08-14-当前数据库快照)
- [9. 自己实操核验](#9-自己实操核验)
  - [9.1 检查应用是否读取生产数据库](#91-检查应用是否读取生产数据库)
  - [9.2 核验上交所 510500 清单数据](#92-核验上交所-510500-清单数据)
  - [9.3 核验深交所 159922](#93-核验深交所-159922)
  - [9.4 核验证监会场外基金 050025](#94-核验证监会场外基金-050025)
  - [9.5 直接核验数据库的最新展示值](#95-直接核验数据库的最新展示值)
  - [9.6 验证同步器但不提交](#96-验证同步器但不提交)
- [10. 准确性核验清单](#10-准确性核验清单)
- [11. 当前已知限制和风险](#11-当前已知限制和风险)
- [12. 对应代码位置](#12-对应代码位置)

## 1. 结论

当前生产数据来自三个官方机构，没有使用天天基金、东方财富、雪球等第三方聚合数据：

1. 上海证券交易所：上交所 ETF 清单、当前规模和日行情。
2. 深圳证券交易所：深交所 ETF 清单和日行情。
3. 中国证监会：公募基金产品索引，以及基金电子披露平台中的场外基金详情、份额和净值。

当前已经自动采集或生成的数据是：

- 基金代码、名称、管理人、场内/场外、交易所、份额类别、币种；
- 基金所属指数分组、产品结构和部分精确基准描述；
- 场内 ETF 收盘价及交易日期；
- 场外基金单位净值、累计净值及净值日期；
- 上交所 ETF 当前规模；
- 在收盘价与单位净值日期相同时计算的同日估算偏离；
- 运作费率的展示公式已经实现，但只有管理费和托管费都有数据时才会计算。

当前**没有采集任务**的数据是：

- 管理费、托管费、销售服务费；
- 近 1 月、今年以来、近 1 年等收益率；
- 近 1 年跟踪误差；
- 场外基金规模；
- 深交所 ETF 的人民币资产规模；
- 场内 ETF 每日单位净值；
- 指数每日点位以及用于独立复算收益率、跟踪误差的基准序列。

因此，页面后几列出现 `—` 或“待核验”通常不是读取失败，而是数据库对应表目前没有数据。

### “最后同步”时间与业务日期

- “共有 XX 条基金”后的“最后同步”按当前指数类别单独计算，取该类别相关基金、份额、上市信息、净值、行情、规模、费率和指标记录中最大的 `collected_at`。
- 这个时间表示最近一次成功提交并写入该类基金数据的同步时间；失败后回滚或使用 `--dry-run` 的命令不会改变它。
- 它不是交易所或证监会数据的官方业务日期。核验数据时仍须使用列内的“净值日”“交易日”“规模日期”。

### 当前没有自动定时同步

仓库只有手工同步命令，PM2 只运行 API；当前用户的 crontab 也是空的。因此数据新鲜度取决于最后一次人工运行同步命令的时间。

## 2. 数据流

```text
上交所 / 深交所 / 证监会官方页面与接口
                    │
                    ▼
backend/app/sync/*.py  下载、解析、筛选、换算、upsert
                    │
                    ▼
PostgreSQL  基金主体 → 份额 → 上市信息 / 净值 / 行情 / 规模
                    │
                    ▼
backend/app/repository.py  选取最新记录、读取指标、进行少量展示计算
                    │
                    ▼
/api/v1/... → React 页面筛选与展示
```

生产环境必须设置 `IFC_DATA_MODE=database`。如果是 `sample`，API 返回的是 `backend/app/data/sample.py` 中的演示数据，不代表数据库或官方实时数据。可先检查：

```bash
curl -fsS http://127.0.0.1:6006/api/v1/health | jq
```

预期生产结果中的 `dataMode` 为 `database`。

## 3. 官方数据源总表

| 来源 | 官方入口 | 实际读取地址 | 用途 |
|---|---|---|---|
| 上交所 ETF 清单 | <https://etf.sse.com.cn/fundlist/> | <https://query.sse.com.cn/commonQuery.do> | 上交所 ETF 代码、简称、扩位简称、管理人、标的指数、上市日期、分类、当前规模 |
| 上交所日行情 | <https://etf.sse.com.cn/fundlist/> | <https://query.sse.com.cn/commonQuery.do> | 开高低收、成交量、成交金额 |
| 深交所 ETF 清单 | <https://www.szse.cn/www/market/product/list/etfList/index.html> | <https://www.szse.cn/api/report/ShowReport/data> | 深交所 ETF 代码、简称、拟合指数、管理人、快照日期 |
| 深交所日行情 | <https://www.szse.cn/www/market/product/list/etfList/index.html> | <https://www.szse.cn/api/report/ShowReport/data> | 收盘价、成交金额及交易日期 |
| 证监会公募基金产品索引 | <https://www.csrc.gov.cn/csrc/c101900/c1029655/content.shtml> | 页面中最新的 `.xlsx` 附件 | 场外基金候选代码、简称、设立日期和产品索引快照日 |
| 证监会基金代码校验 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/validate_fund.do> | 用场外基金代码换取官方 `fundId` |
| 证监会基金详情 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/fund_detail.do> | 基金全称、管理人、合同生效日期 |
| 证监会最新估值日 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/publicDailyReportSearchData.json> | 获取净值查询可用的最大估值日期 |
| 证监会场外净值 | 基金电子披露平台 | <http://eid.csrc.gov.cn/fund/disclose/getPublicFundJZInfoMore.do> | 同一产品下 A/C/E、人民币/美元等份额及其单位净值、累计净值 |

说明：证监会电子披露接口在当前实现中使用 HTTP，不是 HTTPS。接口只传公开基金查询条件，不应向它发送账号、密码或其他敏感数据；重要数据应再与基金管理人公告交叉核验。

## 4. 各同步器如何读取和处理

### 4.1 上交所 ETF 清单和规模

实现文件：`backend/app/sync/sse_funds.py`

请求参数：

```text
GET https://query.sse.com.cn/commonQuery.do
isPagination=true
sqlId=COMMON_JJZWZ_JJLB_L
pageHelp.pageSize=2000
pageHelp.pageNo=1
pageHelp.beginPage=1
pageHelp.endPage=1
CATEGORY=F000
type=inParams
Referer: https://etf.sse.com.cn/fundlist/
```

读取的主要原始字段：

| 上交所字段 | 含义 | 入库位置/处理 |
|---|---|---|
| `FUND_CODE` | 基金代码 | 份额代码、上市代码 |
| `CATEGORY` | ETF 产品细分类别 | 当前不入库；前端按指数映射详情页参数 |
| `FUND_ABBR` | 场内简称 | 上市简称 |
| `FUND_EXPANSION_ABBR` | 扩位简称 | 优先作为页面基金名称 |
| `COMPANY_NAME` | 基金管理人 | 基金主体的管理人 |
| `INDEX_NAME` | 标的指数名称 | 用于目标指数归类 |
| `LISTING_DATE` | 上市日期 | 上市日期，也暂作为场内份额成立日期 |
| `SCALE` | 页面展示的当前规模（亿元） | 乘以 `100,000,000` 后作为人民币金额入库 |

筛选规则：

- `中证小盘500指数`、`中证500指数` → 中证500；
- `标准普尔500指数`、`标普500指数` → 标普500；
- `纳斯达克100指数`、`纳斯达克-100指数` → 纳斯达克100；
- 中证500名称中含“增强、低波、等权、质量、价值、成长、现金流、信息”的产品排除。

写入规则：

- 基金主体唯一键：`sse:<基金代码>`；
- 一个 ETF 建一个基金主体、一个份额和一条上交所上市记录；
- `SCALE × 1 亿` 写入 `fund_scale.amount` 和 `amount_cny`；
- 规模的 `report_date` 当前使用采集当天，而不是交易所返回的独立业务日期；
- 重复同步使用 upsert，不会生成相同代码的重复主体或份额。

上交所详情页还要求 `category`：

- 中证500 ETF：`F112`，跨市场股票 ETF；
- 当前项目中的标普500、纳斯达克100 ETF：`F131`，跨境 ETF。

前端链接映射位于 `frontend/lib/fund-links.ts`。例如：

```text
https://etf.sse.com.cn/fundlist/funddetail/index.shtml?code=510500&category=F112
```

### 4.2 上交所日行情

实现文件：`backend/app/sync/sse_quotes.py`

请求参数：

```text
GET https://query.sse.com.cn/commonQuery.do
sqlId=COMMON_SSE_CP_GPJCTPZ_GPLB_CJGK_MRGK_C
SEC_CODE=<基金代码>
TX_DATE=<YYYY-MM-DD>
```

字段与换算：

| 原始字段 | 入库字段 | 处理 |
|---|---|---|
| `OPEN_PRICE` | `open_price` | 原值 |
| `HIGH_PRICE` | `high_price` | 原值 |
| `LOW_PRICE` | `low_price` | 原值 |
| `CLOSE_PRICE` | `close_price` | 原值 |
| `TRADE_VOL` | `volume` | 官方页面单位为万份，乘以 10,000 |
| `TRADE_AMT` | `turnover_amount` | 官方页面单位为万元，乘以 10,000 |

未指定 `--date` 时，从昨天开始向前最多检查 12 个自然日，跳过周末，并用数据库中代码最小的上交所基金探测最近有数据的交易日。随后对所有上交所上市基金同步同一天行情。

缺少收盘价的基金会列入 `Missing quotes`，不会用前一日数据冒充当日数据。

### 4.3 深交所 ETF 清单

实现文件：`backend/app/sync/szse_funds.py`

请求参数：

```text
GET https://www.szse.cn/api/report/ShowReport/data
SHOWTYPE=JSON
CATALOGID=1945
TABKEY=tab1
PAGENO=<页码>
txtQueryKeyAndJC=<搜索词>
```

代码分别搜索 `中证500`、`标普500`、`纳指`、`纳斯达克100`，读取所有分页，再按证券代码去重。响应中的 `metadata.subname` 作为官方清单快照日期。

字段映射：

| 深交所字段 | 官方列名 | 处理 |
|---|---|---|
| `sys_key` | 证券代码 | 去除 HTML 后作为份额和上市代码 |
| `kzjcurl` | 证券简称 | 去除 HTML 后作为基金名称和上市简称 |
| `nhzs` | 拟合指数 | 用于指数归类 |
| `dqgm` | 当前规模（万份） | **当前不入库**，因为是份额数，不是人民币资产规模 |
| `glrmc` | 基金管理人 | 基金主体的管理人 |

分类规则：

- `nhzs` 以 `000905` 或 `399905` 开头 → 中证500；
- `nhzs` 以 `SPXNTR` 开头 → 标普500净总收益指数；
- `nhzs` 等于 `NDX` → 纳斯达克100；
- 中证500简称中含增强、成长、价值、低波、等权、质量、现金流、信息的产品排除；
- 纳指科技等非 `NDX` 指数不会混入纳斯达克100。

深交所 `dqgm` 明确是万份。当前代码宁可让“基金规模”为空，也不使用份额数冒充亿元资产净值。

### 4.4 深交所日行情

实现文件：`backend/app/sync/szse_quotes.py`

请求参数：

```text
GET https://www.szse.cn/api/report/ShowReport/data
SHOWTYPE=JSON
CATALOGID=1815_fund_child
TABKEY=tab1
txtDm=<基金代码>
```

读取字段：

| 原始字段 | 含义/处理 |
|---|---|
| `jyrq` | 交易日期 |
| `zqdm` | 证券代码 |
| `zqjc` | 证券简称 |
| `ss` | 收盘价，原值入库 |
| `cjje` | 成交金额，官方单位万元；去掉千分位逗号并乘以 10,000 |
| `qss`、`sdf` | 当前读取但没有入库或展示 |

未指定日期时，取第一个有行情基金历史列表的第一条日期作为目标日期。指定日期时，在各基金返回的历史记录中查找同一天；找不到的基金列入 `Missing quotes`。

深交所同步器不会推测开盘价、最高价、最低价、成交量或净值。

### 4.5 证监会场外基金、份额和净值

实现文件：`backend/app/sync/csrc_funds.py`

处理分五步：

1. 打开证监会“公募基金产品索引”页面，自动找到页面中最后一个 `.xlsx` 附件。
2. 从工作簿标题或下载 URL 读取 `截至 YYYYMMDD` 快照日期。
3. 从 Excel 提取基金代码、简称、设立日期，按名称筛出三个目标指数的场外产品。
4. 调用 `validate_fund.do`，用基金代码取得证监会内部 `fundId`；再读取基金详情页。
5. 以官方最大估值日为终点查询最近 45 天净值，从返回行中识别同一产品下的全部份额并入库。

#### Excel 候选筛选

- 基金代码补足为六位数字；
- 代码以 `15、16、50、51、52、56、58` 开头的产品排除，用于避免场内 ETF 和 LOF 重复进入；
- 名称含 `LOF` 排除；
- 名称含 `ETF` 但不含“联接”排除；
- 名称含“ETF联接”归为 `ETF联接基金`，其他目标产品归为 `普通开放式指数基金`；
- 中证500排除增强、量化、优选、智选、行业中性、低波、等权、质量、成长、价值、基本面、ESG、信息、自由现金流、策略；
- 标普500和纳斯达克100排除等权、低波、质量、增强、策略、科技、生物等变体。

#### 详情字段

详情页读取：

- 基金代码；
- 基金简称和全称；
- 基金管理人；
- 基金合同生效日期。

基金主体唯一键为 `csrc:<fundId>`。这使 A/C/E、人民币/美元等多个代码能够合并在同一个基金主体下，而不是错误地当成互不相关的产品。

#### 净值查询与份额识别

净值接口查询条件：

- `iDisplayLength=500`；
- `fundType=all`；
- `fundName` 使用去掉末尾 A/C/E、人民币、美元现汇/现钞后的产品发现名称；
- 查询区间为最大估值日前 45 天至最大估值日；
- 首次无结果时，用 Excel 候选名称重试。

对返回的每一行：

- 必须满足 `row.fund.idStr == 当前 fundId`，防止同名模糊搜索混入其他产品；
- `code` 必须是六位数字；
- `valuationDate` 必须能解析为日期；
- `shareNetValue` 必须大于 0；
- `shortName` 末尾的 A/B/C/E/I/Y 等字母识别为份额类别；
- 名称含“美元”识别为美元，否则为人民币；“现汇”“现钞”单独记录；
- 同一代码、同一净值日重复时只保留一条；
- `shareNetValue` 写入单位净值，`totalNetValue` 写入累计净值。

每个候选基金在数据库嵌套事务中处理。单只基金失败会记录 `WARNING` 并继续其他基金；全部产品都失败时整次任务回滚。`--dry-run` 最后也会回滚。

## 5. 指数归类与基准口径

页面顶部的“中证500、标普500、纳斯达克100”是指数家族；同一家族内可能存在不同的精确基准。

| 页面指数家族 | 当前精确基准定义 | 说明 |
|---|---|---|
| 中证500 | `csi-500-price-cny` | 中证500价格指数，代码 `000905/399905` |
| 标普500 | `sp-500-price-cny` | 标普500价格指数人民币折算口径，仍需逐只合同核验 |
| 标普500 | `sp-500-net-return-cny` | 深交所 `SPXNTR`，净总收益指数人民币折算口径 |
| 纳斯达克100 | `nasdaq-100-price-cny` | `NDX` 价格指数人民币折算口径，仍需逐只合同核验 |

这些指数主数据目前由同步代码硬编码建立，并没有从中证指数、S&P 或 Nasdaq 的指数主数据接口自动下载。场外产品的基准主要根据基金名称归类，不是通过自动解析基金合同得出。因此：

- 中证500的归类相对明确；
- 境外基金的价格/净收益、人民币折算、汇率处理、业绩比较基准权重必须逐只查看基金合同；
- 比较窗口发现同一指数家族下 `benchmark_description` 不同时会给出警告。

## 6. 页面字段从哪里来

| 页面字段 | 数据表/来源 | 读取或计算规则 |
|---|---|---|
| 基金代码 | `fund_listing.ticker` 或 `fund_share_class.code` | 场内优先上市代码，场外使用份额代码 |
| 基金份额名称 | `fund_share_class.display_name` | 场内来自交易所简称；场外来自净值接口 `shortName` |
| 基金公司 | `fund_product.fund_company` | 上交所/深交所管理人或证监会详情“基金管理人” |
| 场内/场外 | `fund_product.trading_venue` | ETF 为仅场内；普通指数基金和 ETF 联接为仅场外 |
| 交易所 | `fund_listing.exchange` | 上交所、深交所；场外为空 |
| 份额类别 | `fund_share_class.share_class` | 场外根据净值接口名称后缀识别；场内通常为空 |
| 币种 | `fund_share_class.currency` | 场外根据份额名称识别美元/人民币；场内当前为人民币 |
| 收盘价/交易日 | 最新 `market_quote` | 按交易日期倒序，日期相同时按记录 ID 倒序取一条 |
| 单位净值/净值日 | 最新 `nav_daily` | 按净值日期倒序，日期相同时按记录 ID 倒序取一条 |
| 管理费 | 当前有效的 `fee_history.management` | 当前无采集任务，所以生产库为空 |
| 托管费 | 当前有效的 `fee_history.custody` | 当前无采集任务，所以生产库为空 |
| 销售服务费 | 当前有效的 `fee_history.sales_service` | 当前无采集任务，所以生产库为空 |
| 运作费率 | API 运行时计算 | `管理费 + 托管费`；任意一项为空则整体为空；不含销售服务费 |
| 近 1 月 | 最新 `calculated_metric.return_1m` | 当前无计算任务 |
| 今年以来 | 最新 `calculated_metric.return_ytd` | 当前无计算任务 |
| 近 1 年 | 最新 `calculated_metric.return_1y` | 当前无计算任务 |
| 跟踪误差近 1 年 | 最新 `calculated_metric.tracking_error_1y` | 当前无计算任务，页面显示“待核验” |
| 基金规模 | 最新 `fund_scale.amount_cny` | API 除以 1 亿后显示；目前只覆盖上交所 ETF |
| 同日估算偏离 | API 运行时计算 | 仅当交易日等于净值日时计算 `(收盘价 ÷ 单位净值 − 1) × 100%`，四舍五入到 4 位小数 |
| 共有 XX 条基金 | 浏览器中的最终筛选结果 | `visibleFunds.length`，会随指数、场内/场外、交易所、份额类别、币种和关键词变化 |
| “共有 XX 条基金”后的最后同步 | 当前指数类别相关记录的最大 `collected_at` | 按指数类别独立计算；反映最近成功写入时间，不是官方业务日期 |

### “最新”记录的具体含义

- 最新净值：`nav_date DESC, id DESC`；
- 最新行情：`trade_date DESC, id DESC`；
- 最新规模：满足份额级或产品级记录后，`report_date DESC, id DESC`；
- 当前费率：生效时间不晚于现在、失效时间为空或晚于现在，再按生效时间和 ID 倒序；
- 最新计算指标：同一指标代码按 `period_end DESC, id DESC`。

## 7. 数据库表与可追溯字段

核心关系：

```text
index_family
  └─ index_definition
       └─ fund_product
            └─ fund_share_class
                 ├─ fund_listing ─ market_quote
                 ├─ nav_daily
                 ├─ fee_history
                 └─ calculated_metric
            └─ fund_scale（也允许挂到具体份额）
```

大多数业务表都有以下来源字段：

| 字段 | 含义 |
|---|---|
| `source_url` | 读取数据的官方页面或接口 |
| `source_time` | 来源本身对应的时间；不同同步器语义不同 |
| `effective_from` | 数据业务生效时间，部分记录才填写 |
| `collected_at` | 程序实际采集时间，通常为 UTC |
| `quality_status` | `verified`、`delayed`、`estimated`、`sample`、`unavailable` |

当前 `source_document` 表为空，同步器没有保存原始响应文件、响应哈希或本地归档路径。这意味着数据库能记录来源 URL 和时间，但还不能在官方网页改版后对历史原文做完全重放审计。严格核验时应在同步当日另存原始 JSON/XLSX，并计算 SHA-256。

## 8. 2026-08-14 当前数据库快照

这是本文编写时的只读统计，不是长期固定值：

| 项目 | 数量/日期 |
|---|---:|
| 基金主体 `fund_product` | 78 |
| 基金份额 `fund_share_class` | 138 |
| 场内上市份额 | 37 |
| 场外份额 | 101 |
| 中证500份额 | 69（上交所 15、深交所 7、场外 47） |
| 标普500份额 | 16（上交所 2、深交所 2、场外 12） |
| 纳斯达克100份额 | 53（上交所 5、深交所 6、场外 42） |
| 净值记录 | 3,375，日期范围 2026-06-29 至 2026-08-13 |
| 行情记录 | 37，交易日为 2026-08-12 |
| 规模记录 | 22，最新日期为 2026-08-12 |
| 费率记录 | 0 |
| 计算指标记录 | 0 |
| 原始来源文档归档 | 0 |

覆盖率：138 个份额中，101 个有净值、37 个有收盘价、22 个有人民币规模。场内和场外字段不同，不能把“没有净值”和“没有收盘价”一概视为错误。

## 9. 自己实操核验

建议每个来源至少抽查一只基金，并按照“官方原始值 → 数据库 → API → 页面”四层比较。

### 9.1 检查应用是否读取生产数据库

```bash
curl -fsS http://127.0.0.1:6006/api/v1/health | jq
curl -fsS http://127.0.0.1:6006/api/v1/indices | jq
curl -fsS http://127.0.0.1:6006/api/v1/indices/csi-500/funds \
  | jq '.items[] | select(.code == "510500")'
```

通过 Nginx 访问时把路径改为：

```text
https://<你的服务器>/indexfund/api/v1/...
```

### 9.2 核验上交所 510500 清单数据

```bash
curl -fsSG 'https://query.sse.com.cn/commonQuery.do' \
  -H 'Referer: https://etf.sse.com.cn/fundlist/' \
  --data-urlencode 'isPagination=true' \
  --data-urlencode 'sqlId=COMMON_JJZWZ_JJLB_L' \
  --data-urlencode 'pageHelp.pageSize=2000' \
  --data-urlencode 'pageHelp.pageNo=1' \
  --data-urlencode 'pageHelp.beginPage=1' \
  --data-urlencode 'pageHelp.endPage=1' \
  --data-urlencode 'CATEGORY=F000' \
  --data-urlencode 'type=inParams' \
  | jq '.result[] | select(.FUND_CODE == "510500")'
```

重点比较 `FUND_EXPANSION_ABBR`、`COMPANY_NAME`、`INDEX_NAME`、`LISTING_DATE`、`SCALE` 和 `CATEGORY`。当前官方返回的 510500 分类应为 `F112`。

核验指定日期收盘价：

```bash
curl -fsSG 'https://query.sse.com.cn/commonQuery.do' \
  -H 'Referer: https://etf.sse.com.cn/fundlist/' \
  --data-urlencode 'sqlId=COMMON_SSE_CP_GPJCTPZ_GPLB_CJGK_MRGK_C' \
  --data-urlencode 'SEC_CODE=510500' \
  --data-urlencode 'TX_DATE=2026-08-12' \
  | jq '.result[0]'
```

把日期换成数据库和页面显示的交易日，比较 `CLOSE_PRICE`。成交量和成交金额还应验证乘以 10,000 后的结果。

### 9.3 核验深交所 159922

```bash
curl -fsSG 'https://www.szse.cn/api/report/ShowReport/data' \
  -H 'Referer: https://www.szse.cn/www/market/product/list/etfList/index.html' \
  --data-urlencode 'SHOWTYPE=JSON' \
  --data-urlencode 'CATALOGID=1945' \
  --data-urlencode 'TABKEY=tab1' \
  --data-urlencode 'PAGENO=1' \
  --data-urlencode 'txtQueryKeyAndJC=159922' \
  | jq '.[0] | {metadata, data}'
```

比较 `sys_key`、`kzjcurl`、`nhzs`、`glrmc`。`dqgm` 是万份，不应与页面亿元规模比较。

行情：

```bash
curl -fsSG 'https://www.szse.cn/api/report/ShowReport/data' \
  -H 'Referer: https://www.szse.cn/www/market/product/list/etfList/index.html' \
  --data-urlencode 'SHOWTYPE=JSON' \
  --data-urlencode 'CATALOGID=1815_fund_child' \
  --data-urlencode 'TABKEY=tab1' \
  --data-urlencode 'txtDm=159922' \
  | jq '.[0].data[0]'
```

比较 `jyrq` 和 `ss`；将 `cjje` 去掉逗号并乘 10,000 后与数据库成交金额比较。

### 9.4 核验证监会场外基金 050025

第一步，在证监会公募基金产品索引页下载最新 Excel，确认：

- 基金代码 `050025`；
- 基金简称；
- 设立日期；
- 工作簿标题中的快照日期。

第二步，获取 `fundId`：

```bash
curl -fsS -X POST \
  --data 'cFundCode=050025' \
  'http://eid.csrc.gov.cn/fund/disclose/validate_fund.do' | jq
```

本文核验时 050025 对应 `fundId=3201`。然后打开：

```text
http://eid.csrc.gov.cn/fund/disclose/fund_detail.do?fundId=3201
```

核对基金全称、基金管理人和基金合同生效日期。

第三步，查看程序实际提交的净值查询和未加工返回值：

```bash
cd backend
.venv/bin/python - <<'PY'
from datetime import date, timedelta
import httpx
from app.sync.csrc_funds import EID_NAV_URL, nav_query_data

end = date.today()
client = httpx.Client(timeout=30, trust_env=False)
response = client.get(
    EID_NAV_URL,
    params={"aoData": nav_query_data("博时标普500ETF联接", end - timedelta(days=45), end)},
)
response.raise_for_status()
for row in response.json().get("aaData", []):
    if str((row.get("fund") or {}).get("idStr")) == "3201":
        print({key: row.get(key) for key in (
            "code", "shortName", "valuationDate", "shareNetValue", "totalNetValue"
        )})
PY
```

将输出中的 A/C 等份额代码、净值日、单位净值、累计净值分别与数据库和页面比较。

### 9.5 直接核验数据库的最新展示值

下面的查询使用项目自己的 `.env`，不会打印数据库密码：

```bash
cd backend
.venv/bin/python - <<'PY'
from sqlalchemy import text
from app.database import get_session_factory

sql = text("""
SELECT
    sh.code,
    sh.display_name,
    p.fund_company,
    l.exchange,
    n.nav_date,
    n.unit_nav,
    q.trade_date,
    q.close_price,
    sc.report_date AS scale_date,
    sc.amount_cny
FROM fund_share_class sh
JOIN fund_product p ON p.id = sh.fund_product_id
LEFT JOIN fund_listing l ON l.fund_share_class_id = sh.id
LEFT JOIN LATERAL (
    SELECT nav_date, unit_nav
    FROM nav_daily
    WHERE fund_share_class_id = sh.id
    ORDER BY nav_date DESC, id DESC
    LIMIT 1
) n ON true
LEFT JOIN LATERAL (
    SELECT trade_date, close_price
    FROM market_quote
    WHERE fund_listing_id = l.id
    ORDER BY trade_date DESC, id DESC
    LIMIT 1
) q ON true
LEFT JOIN LATERAL (
    SELECT report_date, amount_cny
    FROM fund_scale
    WHERE fund_share_class_id = sh.id
       OR (fund_share_class_id IS NULL AND fund_product_id = p.id)
    ORDER BY report_date DESC, id DESC
    LIMIT 1
) sc ON true
WHERE sh.code IN ('510500', '159922', '050025')
ORDER BY sh.code
""")

with get_session_factory()() as session:
    for row in session.execute(sql).mappings():
        print(dict(row))
PY
```

### 9.6 验证同步器但不提交

在 Home Server、确认没有另一个同步任务运行时执行：

```bash
cd backend
.venv/bin/python -m app.sync.sse_funds --dry-run
.venv/bin/python -m app.sync.sse_quotes --dry-run
.venv/bin/python -m app.sync.szse_funds --dry-run
.venv/bin/python -m app.sync.szse_quotes --dry-run
.venv/bin/python -m app.sync.csrc_funds --dry-run --limit 3
```

`--dry-run` 仍会连接共享数据库并在事务中执行 upsert，只是最后回滚；不要与正式同步并发。证监会全量验证较慢，先用 `--limit 3` 抽查，再决定是否全量执行。

## 10. 准确性核验清单

抽查一只基金时至少确认：

1. API 的 `dataMode` 是 `database`。
2. 基金代码、份额代码、交易所和管理人与官方一致。
3. 指数名称是目标指数本身，不是增强、等权、低波、科技等变体。
4. 境外基金的价格指数、净收益指数、人民币折算和汇率口径已查看基金合同。
5. 收盘价使用页面显示的同一交易日。
6. 单位净值使用页面显示的同一净值日，并区分 A/C/E 与人民币/美元份额。
7. 深交所 `dqgm` 没有被误当成人民币亿元规模。
8. 上交所规模验证了原始 `SCALE` 与数据库 `amount_cny ÷ 1 亿` 相等。
9. 同日估算偏离只在交易日和净值日相同的情况下出现。
10. 空白费率、收益率、跟踪误差没有被误解成 0。
11. “最后同步”与本次同步提交时间相符，并且没有被当成官方行情、净值或规模日期。
12. 记录的 `source_url`、`source_time`、`collected_at` 与本次采集相符。

## 11. 当前已知限制和风险

1. **没有定时任务**：数据不会自动每天更新。
2. **缺少原始响应归档**：`source_document` 为空，无法对历史原文做完全重放审计。
3. **缺少费率与指标管道**：费率、收益率、跟踪误差为空。
4. **场内净值缺失**：交易所行情同步只提供价格，不提供可与价格同日比较的 ETF 单位净值。
5. **规模覆盖不完整**：只把上交所的亿元规模入库；深交所万份规模和场外规模不展示。
6. **境外精确基准未逐只核验**：当前主要依赖交易所指数代码和基金名称，没有自动解析基金合同。
7. **场外归类依赖名称规则**：即使有排除词，仍可能遇到新命名方式，需要人工抽查。
8. **缺少下架清理**：同步器只新增或更新，没有把官方清单中已消失的基金自动标记为 inactive/delisted。
9. **官方接口并非公开稳定 API**：`sqlId`、`CATALOGID`、HTML 结构或字段名变化都会导致采集器失效。
10. **来源时间语义不完全统一**：上交所清单和规模目前以采集时间/采集日记录，深交所清单使用官方快照日，证监会产品使用 Excel 快照日，净值和行情另有业务日期。
11. **最后同步是写入时间的推导值**：当前取相关数据表中最大的 `collected_at`，尚无独立的同步运行日志；失败任务、`--dry-run` 和未写入任何记录的空任务不会显示。
12. **`source_name` 通常为空**：同步器直接写 `source_url`，没有创建 `source_document` 记录，因此 API 不一定返回来源名称。

## 12. 对应代码位置

| 主题 | 文件 |
|---|---|
| 上交所基金清单与规模 | `backend/app/sync/sse_funds.py` |
| 上交所行情 | `backend/app/sync/sse_quotes.py` |
| 深交所基金清单 | `backend/app/sync/szse_funds.py` |
| 深交所行情 | `backend/app/sync/szse_quotes.py` |
| 证监会场外基金与净值 | `backend/app/sync/csrc_funds.py` |
| 数据库表和约束 | `backend/app/database_models.py` |
| 最新记录选择与页面计算 | `backend/app/repository.py` |
| API 路由和筛选 | `backend/app/main.py` |
| 页面字段类型 | `frontend/lib/types.ts` |
| 上交所/深交所详情链接 | `frontend/lib/fund-links.ts` |
| 同步器回归测试 | `backend/tests/test_*sync.py`、`backend/tests/test_*quotes.py` |
| 展示计算回归测试 | `backend/tests/test_repository_metrics.py` |

